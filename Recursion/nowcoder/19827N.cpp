#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int inf = 0x3f3f3f3f;
const ll maxn = 1e5 + 10;
int k;
int a, b;
int permutation[1025];
//
bool judge(int a[])
{
    return true;
}
int fun(int a[])
{
    if (!judge(a))
    {
        return -1;
    }
    else
}
int main()
{
    cin >> k;
    cin >> a >> b;
    for (int i = 0; i < pow(2, k); i++)
    {
        cin >> permutation[i];
    }

    return 0;
}
